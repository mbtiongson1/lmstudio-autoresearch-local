# Improve LM Studio Client

## 1. System Prompt Update
Update the  to support passing a  correctly to all API requests, not just during initialization.

## 2. Research Output Enhancement
Update  or  to output event progress (e.g., ) so the user can see model activity.

## 3. API Integration
Adopt the  method from  which properly handles tool calls and provides debugging logs, merging it into the main codebase.
